# 教训

## 2026-07-26：不可逆购买后的退出门槛不能继续沿用买入前 ROI

`PT-20260726-4614945d82` 在 Steam 购买步骤完成后，C5 上架前重新计算得到原始 ROI `0.0229799318`，人工批准时冻结的四位小数 ROI 为 `0.0230`。买 B 前代码按四位小数判断二者相等并放行，买 B 后却使用未经归一化的浮点数直接判断，导致页面出现 `2.30% < 2.30%`，并把已经买入的流水错误转为 `manual_required`。

固定规则：

- 买 B 前属于可逆决策，继续使用自动最低 ROI 或人工批准 ROI，统一按四位小数比较。
- 进入 `steam_bought` 后购买已经不可逆，不能再因为低于开单 ROI 而把资产卡在中间；C5 上架前只要求四位小数归一化后的 ROI 不小于 0。
- 高 ROI 人工审核同样属于买入前价格源保护，不能在已经买 B 后阻止 C5 上架。
- C5 价格不可读、市场深度失败、资产证据或订单安全异常仍可阻止上架；“买后只看非负 ROI”不绕过这些非 ROI 风控。
- ROI 拦截文案必须显示足够精度，并同时记录原始 ROI、四位小数门禁值、门槛和门槛来源。
- 恢复此类流水时只能回到 `steam_bought` 并重试 C5 上架，禁止再次购买 B；必须保留追加式误判恢复审计。

核心纪律：

> 风控必须放在不可逆动作之前；不可逆购买已经发生后，应优先完成安全闭环，不能继续用买入门槛把真实资产悬在流程中间。

## 2026-07-26：完整最低价不能由带筛选条件的逐单搜索代替 `price_batch`

### 事故现象与证据

`M4A4 | Hellish (Field-Tested)`（炼狱之火，久经沙场）同一时刻的真实结果是：

```text
price_batch 原始最低价：                    CNY 134.99
首次挂价：134.99 * 0.9967 后向下取分：      CNY 134.54
market/v2/products/search + acceptBargain=false：CNY 139.54
```

程序错误采用 CNY 139.54 及附近的残缺逐单结果，制造了约 5.19%～6.61% 的虚假 ROI，并导致炼狱之火从凌晨 3 点起出现多笔错误中断。正确的首次目标挂价始终应是 CNY 134.54。

### 本质原因

把“寻找某类商品”的搜索筛选条件复制进了“取得完整市场最低价”的定价路径。测试只验证了请求体和局部样例，没有用同一物品的真实完整市场结果验证最低价语义，因此漏价没有被测试发现。

以下任一筛选都会把完整市场切成子集：

- 传 `delivery=1`：只查询人工发货，漏掉 `delivery=2` 的自动发货挂单。
- 传 `delivery=2`：只查询自动发货，漏掉 `delivery=1` 的人工发货挂单。
- 传 `acceptBargain=false`：漏掉允许还价、但仍然可以按标价直接购买的正常挂单。
- 传 `acceptBargain=true`：反过来只看允许还价的挂单，同样不是完整市场。
- 正确的逐单市场请求只能按物品和价格范围查询，不得用上述字段缩小市场。“寻找适合程序直接批量购买的商品”同样不得传 `delivery` 和 `acceptBargain`。

### 固定实现规则

- Profit Trade 的 C5 最低价只认 `merchant/product/price/batch`（`price_batch`）。扫描机会、Steam `search_listings` 400/429 求购兜底、真正调用 `sale_create` 前复查、自动改价统一复用这一来源。
- 删除 `goods_search`，禁止逐单搜索、统计价和分页结果覆盖 `price_batch`。
- `market/v2/products/search` 只保留给确实需要逐张 `productId` 的购买路径，请求不得包含 `delivery` 或 `acceptBargain`。
- `price_batch` 只返回品类级最低价、数量和 `itemId`，不返回逐张挂单的 `productId`；所以不能声称已经从它的聚合值中精确删除了某张自家挂单。
- 自动改价必须先收集数据库中全部已知的同饰品自家活跃 Profit Trade 挂价。`price_batch` 最低价命中任一自家挂价时，认为最低价可能就是自己，所有相关挂单保持原价；只有最低价严格低于全部已知自家挂价时才允许追价。多张自家挂单不得互相追着降价。
- 上述保护只能识别程序数据库中已知的 Profit Trade 挂单；`price_batch` 本身无法识别账号外手工挂单。证据不足时选择不自动降价，不能假装得到了排除自己后的第二最低价。

### 回归测试纪律

- 不能只断言代码“调用了哪个接口”或请求体“不含哪个字段”；必须验证真实业务语义：同一物品同时存在允许还价、不同发货方式和多张挂单时，最终最低价不能被抬高。
- 固定保留本次精确样本：`price_batch=134.99`、错误筛选结果 `139.54`、首次目标挂价 `134.54`，并验证后者不能覆盖前者。
- 固定覆盖自我竞争：自己挂 CNY 95、`price_batch` 也为 CNY 95 时不得改价；同饰品自家分别挂 CNY 95 和 CNY 100 时，两张都不得追着 CNY 95 再降 1%。
- 每次修改最低价来源、分页或过滤参数后，都必须同时跑扫描、400/429 fallback、首次上架和自动改价回归。

核心纪律：

> 定价接口必须回答“完整市场最低价”，不能回答“某个筛选子集的最低价”；聚合接口无法识别自己的挂单时，宁可保守不降，也不能追着自己打价格战。

## 2026-07-25：稳态价模型不能建立在残缺盘口上，统计参考价不得掩盖真实最低竞争价

### 事故现象

`Sawed-Off | Wasteland Princess (Field-Tested)` 在 C5 App 中的实际在售价依次包含：

```text
216.51
216.92
219.78
219.78
219.98（多件）
227.xx（多件）
```

旧 Profit Trade 却把 C5 稳态竞争参考价识别为 CNY 227.96，首次目标挂价因此变成 CNY 227.20，并把这笔机会计算成约 7.57% ROI。完整盘口明显存在 CNY 216～219 的真实竞争价格带，CNY 227.96 不可能代表买家实际面对的最低竞争价。

### 根因

这是“输入残缺”和“算法缺少跳档保护”两个缺陷叠加造成的开单决策错误：

1. `_fetch_c5_listing_depth()` 调用 C5 `market/v2/products/search` 时写死 `delivery=1`，但 C5 App 会把不同 `delivery` 类型的挂单合并展示。程序因此漏掉了 `delivery=2` 中的 CNY 216.51、219.78、219.98 等真实低价挂单。
2. 稳态墙算法只判断某个局部价格区间内是否达到数量和价差条件，不检查该墙距离完整市场最低价有多远，也不检查中间是否已经存在连续价格带。
3. 当残缺输入只剩 CNY 216.92、225.22、227.96、227.96……时，前两档被当作孤价跳过，算法便把远处的 CNY 227.96 识别成稳态墙。
4. 错误参考价直接进入首次上架价、预计净到手、利润和 ROI，并改变是否锁 A、买 B、上架 C5 的真实执行决策。这不是展示误差，而是可能让本应低于最低 ROI 的交易被错误执行。

### 当前固定实现规则

- 当前不再使用逐单盘口生成 Profit Trade 参考价；C5 最低价唯一使用 `price_batch`，不得再把任一 `delivery` / `acceptBargain` 子集或统计价当作完整市场。
- 当前停用稳定价格墙、独立孤价和三小时孤价确认；首次上架和后续改价统一使用 `price_batch` 最低价。
- 自动改价中，`price_batch` 最低价命中任一已知自家挂价时必须保持原价，不能追着自己的聚合最低价下降。
- `price_batch` 没有可用最低价时必须停止本件执行，不能回退到较高的统计价制造虚假 ROI。
- 必须保留包含 CNY 216.51、216.92、219.78、219.98、227.96 的精确回归用例，验证参考价始终为 CNY 216.51，而不是 CNY 227.96。

### 将来重新设计稳态价的前置条件

如果以后再次希望过滤短暂孤价，不能直接恢复这次的稳态墙算法。至少先满足以下条件：

1. 先证明输入是 C5 App 实际可见的完整竞争市场，明确所有筛选参数、`delivery` 类型、分页、去重和自家挂单排除规则。
2. 同时保存并展示原始最低价、最终参考价、被跳过的每一档价格及跳过理由，不能只展示一个“稳态价”结果。
3. 给参考价相对原始最低价设置跳档保护；价差足以改变最低 ROI 或执行资格时，必须停止自动交易并提示人工检查，不能自动向上选择远端价格墙。
4. 使用真实历史盘口进行离线回放和影子观察，确认一段时间内不会抬高 ROI、不会把卖不出去的高价当成可执行收益后，才能重新进入真实交易路径。
5. 回归测试必须覆盖：不同 `delivery` 混排、连续低价带、单件孤价、多件价格墙、自家挂单位于最低档、分页边界和市场快速变化。

核心纪律：

> 任何“稳态”“去噪”或统计模型，都必须先证明输入覆盖了用户实际面对的完整市场；原始数据不完整时，模型越聪明，错误越隐蔽、风险越大。

## 2026-07-23：Steam 求购价是最高愿付价，低价成交不得误判并重复购买

### 事故现象

Profit Trade 对 `Sticker | Hot Pepper (Holo)` 创建了最高价为 CNY 9.77、数量为 1 的 Steam 求购。Steam 按卖方实际挂牌价 CNY 9.44 完成了第一笔成交，钱包因此只减少 CNY 9.44。

旧代码错误地要求钱包扣款必须接近或达到最高求购价 CNY 9.77，于是把合法的低价成交判成“purchase completion is not verified”。这笔流水进入 `manual_required` 后，A 资产的 60 秒预约没有延长为终态锁；预约过期后，同一个 A 再次进入扫描，程序又创建了一笔 CNY 9.77 的求购并再次成交，造成重复购买和真实资金损失。

### 根因

错误同时存在于业务语义和状态机隔离两层：

1. 把 `createbuyorder` 的最高愿付价当成实际成交价。Steam 买单只规定最大支付金额；匹配到更便宜的卖单时，买方只支付卖单实际价格。
2. 钱包确认使用了错误的不等式：`wallet_delta + tolerance >= buy_order_max`。合法低价成交必然可能不满足这个条件。
3. Steam 官方购买回执也按 `最高求购价 ± 误差` 精确匹配，导致同名、同时间窗口、实际付款更低的真实 `event_type=4` 被过滤。
4. 已经发送真实购买请求但终态不确定时，A 仍沿用扫描阶段的短预约；预约超时释放后，同一 A 能创建第二笔 Profit Trade。
5. 如果继续重试并始终拿最初钱包余额比较，多次购买的累计扣款甚至可能“凑够”最高求购价，从而把重复购买错误地验证为成功。

### Steam 官方规则

- 买单价格是愿意支付的最大金额，不是必须支付的固定金额。
- 商品以更低价格出现时，实际只支付该更低价格。
- 多个买单都满足新卖单时，Steam 选择最早的匹配买单，而不是价格最高的买单。
- 官方说明：`https://help.steampowered.com/en/faqs/view/61F0-72B7-9A18-C70B`

### 固定实现规则

- `createbuyorder` 必须分别记录：`steamBuyMaximumPrice`（最高愿付价）和 `steamBuyActualPrice`（实际成交价）。
- 对求购成交，合法钱包扣款区间是 `0 < wallet_delta <= maximum_price + currency_tolerance`；不能要求钱包扣满最高求购价。
- 低于最高求购价的钱包扣款只能作为“可能已低价成交”的证据，还要绑定同名新库存资产或 Steam 官方 `event_type=4` 购买回执，避免把账号同时发生的其他钱包变化误认成本单成交。
- 官方购买回执对求购单使用“实际付款不超过最高愿付价”的上界匹配；只有直接指定卖单购买才使用预期总价的精确匹配。
- 低价成交确认后，成本、预计收益和 ROI 必须按实际付款重新计算，不得继续按最高求购价记账。
- 只要真实购买请求已经发送，而成交/撤单终态仍不确定，A 预约必须立即改为无到期时间。只有确认成交后推进状态，或确认远端求购已撤销且不存在成交证据后，才能消费或释放预约。
- 出现钱包减少但库存/history 暂时延迟时，必须停止本单重试并继续对账；禁止为了等待证据再次购买，禁止释放 A，禁止让同一 A 产生新流水。
- 三次求购重试只适用于每次都已明确证明“上一笔没有成交且远端求购已撤销”的情况。任何钱包扣款、同名新资产、官方购买事件或终态冲突都会立即终止重试。

### 类比到其他项目

- 限价单、广告竞价和采购上限都是“最多支付”，不能拿上限精确匹配实际结算金额。
- 支付、下单、库存扣减等有真实副作用的请求一旦发出，终态未知时必须保留幂等键和业务锁，不能因本地超时释放资源后重新执行。
- 重试前必须证明上一请求明确失败；“没有立即读到成功结果”不等于“没有成功”。

核心纪律：

> 最高授权金额不是实际结算金额；真实副作用一旦可能发生，终态未知时宁可锁住并持续对账，也绝不能释放业务对象后再次执行。

## 2026-07-02：请求成功不等于业务成功

### 触发场景

搬砖做T的买 B 步骤曾经把 Steam `createbuyorder` 接口返回成功，直接当成“B 已经买到”，随后继续推进到 C5 上架 A 和收益结算。

实际情况是：`createbuyorder` 只代表求购单创建成功；它可能立刻成交，也可能只是挂着等待成交。之前少了“成交/入库/扣款”验证，导致 A 已经卖出，但 B 没有补回来。

### 根因

把“外部系统接受请求”误当成“业务事实已经发生”。

这是状态机类项目里非常危险的错误。交易、支付、库存、部署、消息发送、文件同步等动作，都可能存在：

- 请求已提交
- 外部系统已接受
- 等待中
- 最终成功
- 最终失败
- 状态未知

这些状态不能混成一个 `success`。

### 固定规则

以后任何状态机推进到下一步之前，必须验证当前步骤的业务事实已经成立。

- 买入类动作：只有确认钱包扣款、成交记录、资产入库、或可靠的外部系统最终状态后，才能进入 `bought/completed`。
- 求购类动作：`createbuyorder` 成功只能说明“求购单创建成功”。如果还存在未成交求购单，只能进入 `pending/uncertain/manual_required`，不能进入“已买入”。
- 带二次确认的求购类动作：Steam Guard 确认成功也不等于买入成功。确认后如果接口返回“已有有效订购单”等模糊错误，必须立刻重新查钱包、库存和活跃求购单；它可能已经成交，也可能只是挂单或失败。
- 上架类动作：上架接口成功后，还要确认远端在售记录或明确的产品 ID，才能认为“已上架”。
- 支付类动作：支付请求返回成功后，还要确认账单状态、回调状态或账户余额变化，才能认为“已支付”。
- 部署类动作：构建或提交成功后，还要确认线上版本、健康检查或访问结果，才能认为“已上线”。
- 消息类动作：发送 API 成功后，如果后续流程依赖对方收到，必须检查投递状态或回执，不能只看 HTTP 200。

### 实施口径

状态命名必须区分：

- `accepted`：请求被接收
- `pending`：等待外部系统完成
- `completed`：业务事实已验证
- `failed`：明确失败
- `uncertain/manual_required`：无法确认，禁止继续推进依赖后续动作

写代码时，外部接口返回 `success` 后也要继续问一句：

> 这个 `success` 证明了什么？它证明的是“请求成功”，还是“最终业务结果成功”？

如果只能证明请求成功，就不能推进最终状态。

### 追加教训：补救动作前先查现状

补救事故时，不能只根据旧记录判断“还缺什么”。外部系统里的旧求购单可能在之后成交，库存可能已经恢复。

补买、重试、重发、重部署之前，必须先做一次现状确认：

- 原始账号现在是否已经到账/入库？
- 旧 pending 单是否还存在？
- 钱包、订单、库存三者是否已经说明问题自然闭环？
- 如果已经闭环，补救动作必须停止，不能再重复执行。

这条规则适用于所有项目：重试前先查终态。不要把“旧日志里的失败”当成“当前仍失败”。

### 类比到其他项目

以后重构任何项目，只要出现“调用外部系统后推进本地状态”，都要套用这条规则。

- 电商：创建订单成功不等于付款成功。
- 支付：扣款请求成功不等于入账成功。
- 库存：提交出库请求不等于库存已经扣减。
- 自动化交易：下单成功不等于成交成功。
- CI/CD：构建成功不等于生产环境可用。
- 通知系统：发送请求成功不等于用户已收到。
- 数据同步：同步任务启动成功不等于目标数据已一致。

默认策略是：宁愿停在 `pending/uncertain`，也不要把未验证的结果推进成 `completed`。

## 2026-07-10：设计日期筛选前必须先定义时间字段

### 触发场景

搬砖做T前端给“已完结收益”增加日期筛选时，直接复用了流水的 `completedAt`。随后又在同一个筛选区域增加“Steam买入合计”，但没有重新确认这个指标应该按什么时间筛选。

结果是：一笔在筛选开始日期以前买入、但在筛选范围内完成收益结算的流水，被计入了“当前筛选 Steam买入”；而筛选范围内真实买入、尚未完成收益结算的流水又没有进入这个区域。加法本身没有错，错的是没有先定义时间口径，导致页面名称和用户理解不一致。

### 根因

把“日期范围”当成天然明确的条件，没有先回答以下问题：

- 筛选的业务对象是什么？
- 这个指标对应哪一个业务事件？
- 应使用哪个时间字段？
- 时间来自本地状态推进，还是外部系统官方回执？
- 时区和起止边界是什么？
- 时间字段缺失时应该排除、提示，还是允许明确的兼容回退？

同一笔交易可能同时拥有多条时间轴：

- 机会发现时间
- A资产锁定时间
- Steam买入B时间
- C5上架时间
- C5售出时间
- 收益结算时间
- 程序最后更新时间

这些时间不能互相替代。`createdAt`、`updatedAt`、`completedAt` 也不能因为“肯定存在”就被当成所有报表的通用时间。

### 固定规则

以后设计任何日期筛选、报表汇总或时间范围接口之前，必须先写清楚四项口径：

1. **业务事件**：到底统计买入、卖出、上架、结算还是程序处理。
2. **时间字段**：明确使用 `steamBoughtAt`、`steamSoldAt`、`c5SoldAt`、`completedAt` 等哪个字段。
3. **数据来源**：优先使用外部系统官方事件时间；程序确认时间只能描述程序何时发现结果。
4. **缺失处理**：缺少目标时间字段时默认排除并单独提示，禁止静默回退到其他时间字段。

指标与时间字段必须一一对应：

- Steam钱包购买扣款：按 Steam 官方购买时间 / 已验证的 `steamBoughtAt`。
- Steam钱包卖出入账：按 Steam 官方 `steamSoldAt/timeSold`。
- C5实际售出金额：按 C5官方售出时间。
- 已实现利润：根据页面目的明确按“Steam买入批次”或“收益结算批次”，页面标题必须直接写出口径。
- C5补仓报表：按 C5补仓完成时间。

同一个日期控件不能在没有说明的情况下同时控制不同时间口径的指标。如果页面需要同时展示“按购买时间的投入”和“按结算时间的利润”，应拆成两个筛选区域，或提供明确的“时间口径”选择器。

### UI与测试要求

- 标签不能只写“当前筛选”，必须写明“按 Steam购买时间”“按 C5售出时间”或“按收益结算时间”。
- 卡片应展示参与筛选的实际时间，方便用户肉眼核对。
- 必须增加跨边界测试：
  - 窗口前买入、窗口内结算，按购买时间筛选时必须排除。
  - 窗口内买入、窗口后结算，按购买时间统计钱包花费时必须计入。
  - 目标时间缺失时不能用 `completedAt/updatedAt` 偷偷补位。
- 测试不仅要验证总金额，还要验证具体哪一笔被包含或排除。

### 类比到其他项目

这个教训适用于所有包含生命周期和报表的系统：

- 电商：下单时间、支付时间、发货时间、退款时间不能混用。
- 支付：发起扣款时间、实际入账时间、对账确认时间不能混用。
- 物流：创建运单、揽收、签收、系统同步时间不能混用。
- CI/CD：提交、构建完成、部署、健康检查通过时间不能混用。
- 数据同步：任务启动、源数据更新时间、目标写入完成时间不能混用。

设计日期筛选前必须先问：

> 用户选择这个日期范围时，真正想圈定的是哪个业务事件？

只有答案明确后，才能开始写筛选代码和汇总指标。

## 2026-07-10：重试之前必须确认上一笔远端动作已经终结

### 事故

Steam `createbuyorder` 创建求购单后，程序虽然尝试调用了 `cancelbuyorder`，但只要撤单接口返回 `success`，就直接认为求购已经撤销并进入下一次重试；“已知晓并隐藏”也只取消本地卡片，没有检查远端求购单。

结果是：旧求购单仍可能留在 Steam，之后自行成交。本地卡片已经隐藏，程序既不会继续跟踪，也可能再次买入同一个 B，形成漏记账或重复购买。

### 固定规则

- 保留业务原本约定的失败重试次数；修复安全问题不能偷偷删除、缩减或绕过重试。
- 每次重试内部必须形成独立闭环：创建请求 → 等待并确认成交 → 未成交才撤单 → 确认远端订单已消失 → 才允许进入下一次重试。
- `cancel` 接口返回成功，只能证明“撤单请求被接受”，不能单独证明订单最终未成交。
- 撤单后必须同时排除竞态：如果钱包扣款、Steam 官方购买历史或新增资产证明已经成交，应按成交成功继续，禁止再次购买。
- 撤单后订单仍活跃、接口不可读或证据冲突时，状态必须保留为 `uncertain/manual_required`，A 资产继续锁定，不得继续重试。
- “隐藏”不是纯 UI 操作。只要卡片关联远端 pending order，隐藏前就必须安全撤销并确认终态；无法确认时必须拒绝隐藏。
- 后续循环只审计确实存在未确认远端订单的流水。已经确认撤销的订单要记录终态并跳过，避免每轮无意义请求拖慢程序。

### 类比到其他项目

凡是“失败后重试”的外部动作，都必须先回答：上一笔请求是真的失败、已取消，还是仍可能在远端完成？支付、下单、发消息、部署、库存扣减、退款都适用同一规则。没有终态证据就重试，会把一次不确定动作放大成重复扣款、重复订单或重复执行。

## 2026-07-11：失败状态必须有恢复条件，不能和业务终态一起永久保留

### 事故

两件 `Kilowatt Case` 已经重新出现在 C5 实时库存中，且 `ifTradable=true`，但挂刀执行器始终显示“C5聚合可交易 2 件，本地可上架 0 件”。数据库里这两件资产都是 `listing_failed`，没有关联的 `pool_operations`，也没有活动 `asset_reservations`。

库存同步的 upsert 把 `listing_failed` 与 `listed`、`sold`、`listing_pending` 一起永久保留。结果是：外部系统已经再次证明资产真实存在并且可交易，本地历史失败标记仍拥有更高优先级，资产永远无法重新进入候选。

### 根因

状态设计没有区分两类语义：

- `listed`、`sold`、`listing_pending` 表示已经发生或可能已经发生远端业务动作，必须谨慎保留，不能由库存刷新覆盖。
- `listing_failed` 只表示某次尝试失败，不是资产的永久业务终态；它必须带失败证据、重试时间和恢复条件。

同时，旧实现只修改 `inventory_assets.status`，没有写失败流水和错误原因，后续既无法判断为什么失败，也无法安全决定何时重试。

### 固定规则

- 失败状态必须回答三个问题：失败原因是什么、何时允许重试、由什么证据恢复。
- 外部实时库存再次确认资产 `ifTradable=true`，且该资产没有活动挂单流水、待确认动作或资产预留时，孤立的历史 `listing_failed` 可以恢复为 `available`。
- 非瞬时上架失败也要写入带 `deferReason`、`deferMessage`、`deferredUntil` 的流水；冷却期内跳过，冷却到期后才允许重试。
- 不允许简单地每轮把失败资产恢复并立刻重试，否则高频循环会反复请求 Steam；也不允许永久保留失败状态，否则一次偶发失败会永久吃掉库存。
- `listed`、`sold`、`listing_pending` 仍属于受保护状态，库存刷新不能擅自覆盖。
- 修状态机问题时必须用“外部当前事实 + 本地活动流水 + 本地锁/预留”共同判断，不能只看一个历史状态字段。

### 类比到其他项目

支付失败、发货失败、部署失败、消息发送失败、库存同步失败都不能只留下一个永久 `failed`。只要失败是可重试的，就必须保存错误证据、退避时间和恢复条件；真正需要永久阻断的状态必须有人工解除或明确终态证据。

## 2026-07-12：页面局部样式不能绕开既有设计系统

### 问题

挂刀报表新增排序按钮时，在页面组件内临时增加了 `.report-table-actions`，并把普通排序操作做成了主操作样式。功能可以运行，但它把通用的“面板底部操作区”误写成页面专属实现，也没有根据操作重要性选择 FOLIO 按钮层级。

### 固定规则

- 先判断样式表达的是业务特例还是可复用布局模式；面板头部、底部操作区、表格工具栏等通用模式应进入全局设计系统。
- 页面组件的 `<style scoped>` 只用于确实无法复用、只属于该组件内部结构的样式，不能用它绕过共享视觉约定。
- 按钮层级必须匹配业务语义：执行、保存等主要动作使用主按钮；排序、筛选、展开等可逆辅助操作使用次级按钮；删除、真实交易等危险动作使用危险样式。
- 新增样式前先搜索现有全局类和同类页面；已有模式直接复用，没有模式且预计可复用时再新增语义化共享类。
- 类名描述布局职责，不绑定具体页面，例如使用 `.panel-footer-actions`，避免产生 `.report-table-actions`、`.table-buttons` 等重复变体。

### 类比到其他项目

设计系统的价值不只是统一颜色。组件归属、交互层级、命名和复用边界也必须统一。局部样式在技术上可行，不代表在大型前端工程中就是正确归属。

## 2026-07-12：真实交易请求超时不是失败，必须按不确定终态对账

### 事故

C5 武器箱扫货使用 `/merchant/trade/v1/batch/buy` 一次提交多个 `productId`。程序在两次批量购买中分别收到来自 `openapi.c5game.com` 请求链路的 `504 Gateway Time-out`，旧代码进入异常分支后直接记录“批量购买未被接受”，没有创建本地订单，也没有暂停轮次。

但 C5 内部购买服务实际上已经继续执行：

- 12:04–12:05 的 504 批次，C5 远端成功 51 件，实际金额 CNY 87.72，本地记录 0 件。
- 12:37 的 504 批次，C5 远端成功 37 件，实际金额 CNY 63.27，本地记录 0 件。
- 两次共漏记 88 件、CNY 150.99。

由于本地账本仍认为数量和预算没有占用，后续轮次继续购买，最终目标为 100 件的轮次实际成功 125 件，成功金额达到 CNY 214.90。C5 App 能看到订单，而网页没有记录，是因为远端已经执行成功，但本地没有收到正常的 `successList` 响应。

### 根因

旧实现犯了三个连续错误：

1. 只把待提交的 `productId`、`buyPrice`、`outTradeNo` 保存在函数内存中，没有在真实请求前持久化。
2. 把 HTTP 504、连接超时、断网等“没有收到确定响应”错误解释成“远端确定没有执行”。
3. 异常后既没有锁住预算，也没有暂停状态机，下一轮依据错误的本地状态继续提交真实购买。

HTTP 504 只能证明客户端没有在等待时间内收到完整响应，不能证明 C5 没有收到请求、没有扣款或没有创建订单。网关等待超时后，C5 内部任务仍可能继续执行。任何已经跨过真实交易边界的请求，都可能出现“远端成功、本地未知”的不确定终态。

### 固定规则

- 任何可能产生扣款、购买、上架、卖出、撤单、退款或资产转移的请求，必须在发出前持久化请求凭证，至少包括业务对象 ID、请求唯一号、金额、账号、提交时间和本地流水 ID。
- HTTP 504、读取超时、连接中断、空响应、响应解析失败都属于 `uncertain`，不得直接标记为 `failed`。
- 进入 `uncertain` 后必须立即停止同一业务对象的后续真实执行，保留锁和预算占用；在远端终态确认前禁止重试。
- 批量购买必须保存整批 `productId + buyPrice + outTradeNo`。即使程序在请求过程中崩溃，重启后也必须能恢复这批证据。
- 远端对账优先使用请求前保存的唯一标识。本项目 C5 扫货应按 `productId` 精确匹配买家订单，并同时校验接收 SteamID；不能只按饰品名称、价格或相近时间模糊认领。
- 查到远端成功时，补建本地订单并按远端金额和官方时间记账；查到明确失败时，才能释放预算；暂时查不到时继续保持 `uncertain/paused`。
- 未完成对账的批次必须成为后端硬闸门。前端点击继续、后端重启或下一次定时循环都不能绕过该闸门。
- 批量接口的单次件数应结合服务端处理能力设置上限，以降低网关超时概率；但降低批量件数不能替代不确定终态保护。
- 修复后必须验证“请求只发送一次、远端成功可补回、远端未知不重试、对账完成前不能继续”四个条件，不能只验证正常成功响应。

### 正确状态链路

```text
准备真实请求
  → 先持久化 submission/request evidence
  → 调用远端接口
  → 正常返回：按 success/failed 明细推进
  → 504/超时/断网/空响应：标记 uncertain 并暂停
  → 查询远端官方订单
      → 找到成功：补记成功流水
      → 找到失败：记录失败并释放资源
      → 仍查不到：保持 uncertain，禁止重试
```

### 类比到其他项目

- 支付：扣款接口超时后必须查支付流水，不能立即再次扣款。
- 电商：创建订单超时后必须按幂等号查订单，不能重复下单。
- 库存：扣减请求超时后必须查库存流水，不能再次扣减。
- 消息系统：发送超时不代表对方没收到，重发前必须依赖消息 ID 和幂等策略。
- CI/CD：部署请求超时不代表部署未发生，必须查询目标环境实际状态。
- 云资源：创建实例超时后必须按请求 ID 查询，不能直接再创建一台。

核心纪律：

> 真实副作用请求没有终态证据时，宁可暂停并对账，也不能把“我没收到结果”当成“对方什么都没做”。

## 2026-07-17：C5 quick-buy 返回 HTTP 200 和 outTradeNo，不代表补仓订单已经创建

### 事故现象

挂刀执行器有三笔 `Kilowatt Case` 补仓流水被推进为 `delivery_pending`：

- `13107`
- `14053`
- `14066`

它们都保存了本地生成的 `outTradeNo`，但 C5 返回结果中的关键业务凭证是：

- `orderAssetId = null`
- `orderId = null`
- `payStatus = 2`

程序旧逻辑只判断 `quick-buy` 没有抛异常，便把 HTTP 200 当成业务下单成功，写入 `delivery_pending` 并创建 24 小时发货确认任务。发货详情接口实际只能使用真实 C5 订单号查询，不能使用本地 `outTradeNo`，所以这些流水既无法查询发货详情，也不会自动失败，只会长期空转。

正常成功样本与此明显不同。已经能够直接证明 C5 创建了可查询订单的响应会同时返回：

- C5 资产订单号：`orderAssetId`
- C5 交易订单号：`orderId`
- 当时的支付状态：例如 `payStatus = 1`

但 `payStatus` 只是订单当时所处的支付/处理状态，不是“订单是否存在”的身份证明。只要已经取得真实的资产订单号和交易订单号，就必须承认远端订单存在，再通过 `buyer_order_detail` 判断发货成功、发货中、明确失败或特殊状态；不能因为 `payStatus != 1` 把真实订单过滤掉。

### 根因

旧状态机混淆了四种不同证据：

1. HTTP 200：只证明 HTTP 请求得到了正常响应。
2. `outTradeNo`：由本程序在请求前生成，只是提交幂等号或请求流水号。
3. `orderAssetId`：C5 资产级订单号，可用于后续发货详情查询。
4. `orderId`：C5 交易级订单号。

其中只有 HTTP 200 或 `outTradeNo`，都不能证明 C5 已经创建购买订单。把它们当成 `delivery_pending` 凭证，会同时产生两类风险：

- 没有真实订单却进入 24 小时发货确认，状态永久空转。
- 网络超时、504 或响应不完整时，如果直接按失败重买，远端原订单可能稍后出现，造成重复补仓。

### 固定状态机口径

以后 C5 `quick-buy` 必须使用以下状态链：

```text
调用 C5 quick-buy
  → 请求未发出：保持原本的价格等待或明确失败语义
  → 请求已发出，但超时、504、连接中断、响应解析失败：c5_submission_unconfirmed
  → HTTP 200，但缺少真实订单号：c5_submission_unconfirmed
  → orderAssetId + orderId 齐全：确认订单存在，立即查询 buyer_order_detail
  → detail 已成功：completed
  → detail 明确失败：c5_failed，并创建唯一替换补仓
  → detail 发货中或暂时不可读：delivery_pending，保留真实订单号继续确认
```

订单存在性的硬证据是可查询的真实 C5 订单号，不包括 `payStatus`。`quick-buy` 直接响应需要同时取得：

```text
c5OrderId 存在
+ c5TradeOrderId 存在
```

买家订单列表可能暂时只返回资产级查询 ID。只有经过 `outTradeNo` 精确匹配或完整时间窗口内的安全唯一匹配后，才能把这个单 ID 标记为已认领订单，并继续查询详情补齐另一种 ID；普通历史单 ID 不能自动获得同样信任。

没有任何可安全认领的真实订单 ID 时：

- 不得开始 12 小时发货复查时钟。
- 不得创建 `delivery:<id>` 任务。
- 不得再次购买。
- 必须进入独立的 `c5_submission_unconfirmed`，创建唯一 `reconcile:<id>` 对账任务。

### 对账规则

对账读取 C5 买家订单时必须遵守：

- `outTradeNo` 如果由远端返回并能精确匹配，可作为优先候选；匹配后必须取得至少一个真实可查询订单 ID，并立即读取详情。`payStatus` 不参与订单存在性判断。
- C5 买家订单列表只返回资产订单号时，应继续用资产订单号查询官方详情，从详情中取得真实交易订单号；禁止把同一个 ID 复制成两种订单号。
- 没有精确 `outTradeNo` 时，模糊匹配必须同时校验品类、价格、接收 SteamID 和原提交时间窗口；缺少可解析的远端创建时间，不能仅凭“同名、同价、同账号”认领历史订单。
- 必须排除已经被其他挂刀补仓流水或 C5 扫货流水认领的远端订单，防止同一 C5 订单记到两笔本地流水。
- 查询不能机械限制为前 5 页或其他固定业务页数。必须继续向后分页，直到订单时间已经覆盖 `submittedAt` 前后的匹配窗口，或者 C5 明确返回末页。
- C5 声称仍有后续页却提前返回空页、整页重复、没有新增记录或请求异常时，本轮只能标记为覆盖不完整；不得累计“无订单”次数。
- 没有总页数时，不能因为当前页不足请求的 `pageSize` 就假定已经到末页；必须继续读取到明确空末页或完成时间覆盖。
- 为防异常接口无限返回新行而长期占住 worker，可以设置“单轮安全页预算”，但触顶只能表示本轮覆盖不完整，不能表示查完；下一轮必须从第 1 页用持久化放大的预算重新完整扫描，同时不丢失前页模糊候选或延迟同步到首页的精确订单。预算达到 1000 页安全上限后不得继续指数放大；仍未覆盖时保持 `c5_submission_unconfirmed`、进入慢速复核并显示异常提醒，绝不能累计“无订单”次数或自动替换。
- 模糊候选只有在完整覆盖提交时间窗口后才能认定为“唯一”；`outTradeNo` 精确匹配不受此限制。
- 多个候选、订单 ID 被其他不完整证据占用或其他证据冲突时，进入 `manual_required`，禁止重复购买。

只有连续多次完整、可证明覆盖原提交时间的查询都没有候选，才能判定本次提交未创建订单：

```text
c5_submission_unconfirmed
  → 多次完整对账仍明确无订单
  → 原流水 c5_failed
  → 创建唯一 pending 替换补仓
```

如果 C5 API 始终不可读或分页覆盖不完整：

- 保持 `c5_submission_unconfirmed`，不能创建替换补仓。
- 快速核对达到阈值后，写入持久化异常提醒并进入较慢的持续复核；不能把任务停止在一个永远不再执行的状态。
- 普通短暂未确认不进入“异常与待处理”；只有达到长期核对提醒阈值后才展示，并且提醒必须去重。

只有多个不同候选、订单已被其他流水认领、与 C5 扫货未决提交重叠、订单详情品类或账号证据明确冲突等情况，才进入 `manual_required` 并禁止替换。明确、完整覆盖三次都没有订单不是人工状态，而是自动 `c5_failed` 并创建唯一替换补仓。

### 替换补仓安全规则

- 每个失败流水最多创建一个替换补仓子流水。
- 创建替换流水和回写父流水必须具备数据库级幂等保护，不能依赖“先查一次、再插入一次”的非原子流程。
- 替换单继续继承原流水冻结的补仓价格上限、`maxRebuyRatioAtOpen` 和 `steamNetFactorAtOpen`，不能追当前 C5 实时价。
- 替换任务只能进入正常调度队列，禁止在同一个函数里递归立即购买。
- 同一卖出链路连续三次出现“提交后经对账确认未创建订单”时，转 `manual_required`，禁止继续生成第四代替换，防止 C5 请求风暴。
- 一旦中间某代已经取得可安全认领的真实订单 ID，连续空订单计数必须清零；之后真实订单发货失败属于新的失败类型，不能沿用此前的连续空提交次数。

### 迁移与验证要求

历史 `delivery_pending` 流水如果既没有双订单号，也没有“安全唯一匹配后已认领的单个真实查询 ID”，重启后必须自动迁移为 `c5_submission_unconfirmed`，终止旧的发货确认任务并创建唯一对账任务。迁移不能粗暴直接重买。

修复完成时至少验证：

- HTTP 200 空订单号不会进入 `delivery_pending`。
- 504、超时和连接中断不会直接重买。
- 非 `1` 的 `payStatus` 不会把已有真实订单号的订单误判为不存在。
- 唯一有效订单可以回填真实订单号，立即查询详情，并按成功、失败、发货中或暂时不可读分别推进。
- `buyer_order_detail.status = 10` 即使缺少 `statusName` 也必须识别为发货成功，不能等 12 小时后误判失败并重复补仓。
- 已被其他执行器认领的 C5 订单不会被当前流水重复认领。
- 第 6 页及更后面的订单仍能被查询；分页未覆盖提交时间时不会误判无订单。
- 连续三次完整覆盖且没有订单时自动失败并创建唯一替换，不进入人工状态。
- 明确无订单后只创建一个替换补仓。
- 连续空提交达到上限后转人工，不形成无限替换链。
- 旧 `delivery:<id>` 任务不会与新 `reconcile:<id>` 任务同时运行。

核心纪律：

> `outTradeNo` 证明“我们准备或尝试提交了什么”；真实 C5 订单 ID 证明“C5 已经存在可查询订单”；`buyer_order_detail` 的发货终态证明“这笔订单最终成功、失败还是仍在处理中”。HTTP 状态、订单存在性和发货终态是三层不同证据，不能再用 `payStatus` 把它们混成一个判断。

## 2026-07-18：`search_listings` 429 不能封死已经完善的安全求购路径

### 事故现象

`Glock-18 | Candy Apple (Factory New)` 在 2026-07-18 14:34 左右满足 Profit Trade 执行条件，预计 ROI 约 8.60%。程序已经审计通过并锁定 A，但随后同一 Steam 账号连续三次调用 `search_listings`，全部收到 HTTP 429。旧逻辑因此取消流水并释放 A，没有发送任何 Steam 购买请求；等 listings 冷却后，低价机会已经消失。

### 根因

旧流程错误地把“获取具体 listingId”当成所有品类和所有买入方式的共同前置条件：

```text
orderbook 成功
→ search_listings
→ 同账号 2 秒、4 秒短重试
→ 三次 429
→ 熔断整个买 B 链路
→ 取消流水并释放 A
```

但项目本来已经有完整的 `createbuyorder` 安全状态机：提交求购后会短轮询钱包、活跃求购单、库存和官方成交证据；未成交时先安全撤单并确认终态；撤单与成交竞态按成交成功处理；终态不确定时保留 A 锁并转人工。真正的问题不是“没有其他购买手段”，而是 listings 429 发生在分支选择之前，导致安全求购分支永远到不了。

此外，箱子、胶囊、印花等 commodity 品类原本就确定使用 `createbuyorder`，却仍先请求一次 `search_listings`，白白增加了最容易受限流的路由压力。

### 固定修复口径

普通非 commodity 饰品：

```text
读取 Steam orderbook
→ search_listings 只尝试一次
→ 成功：优先指定 listingId 买入
→ HTTP 429：立即打开 listings 路由熔断，不在同账号连续短重试
→ 重新读取 Steam orderbook
→ 重新读取 C5 行情与深度
→ 重算价格容差、ROI、余额和 C5 风控
→ 仍符合：按当前 orderbook 最低卖价创建 quantity=1 求购单
→ 不再符合：安全取消，不购买
```

箱子、胶囊、印花等本来就使用求购的品类：

```text
读取 Steam orderbook
→ 直接 createbuyorder
→ 完整确认成交/撤单终态
```

它们不得再调用 `search_listings`。

### 熔断边界

- listings 熔断只隔离 `search_listings` 路由，不再禁止创建新流水、锁定 A 或执行安全求购。
- 冷却期间观察池、Steam orderbook、C5 同步和收益结算继续运行；满足条件的新机会可以在重新校验后走求购。
- 冷却到期直接恢复正常状态，不进入 `half_open`，也不为了“判断是否恢复”额外发送一次 listings 探测。下一笔真实且仍符合条件的操作就是恢复后的首次验证：查询成功继续指定卖单购买，查询再次 429 就重新冷却，并让当前机会在重新校验后改走安全求购。
- 页面只在真实 `open` 冷却期间显示冷却提示和冷却结束时间；倒计时结束立即回到正常状态，不再显示“等待恢复探测”。
- 历史上已经因连续 429 取消的中断流水必须保留，不能事后改成成功。

### 不能破坏的安全约束

- `STEAM_BUY_LISTING_RETRY_ATTEMPTS = 3` 仍负责具体购买/求购失败重试，不能因为减少 listings 查询而被删除、缩减或绕过。
- `createbuyorder` HTTP 成功不等于买到 B；必须继续使用钱包、求购单、库存和官方成交记录确认。
- 未成交后必须先撤单并确认远端订单消失，才能进行下一次购买尝试。
- 发现撤单与成交竞态时按购买成功推进，禁止重复购买。
- 价格、ROI、余额或 C5 风控在 429 后不再符合时，禁止拿旧价格强行求购。

> 恢复探测不是免费的健康检查。`search_listings` 本身就是最容易触发 Steam 路由限流的请求，为探测而探测会浪费一次敏感请求，也无法证明下一次真实操作仍不会 429。冷却到期直接放行正常业务请求，失败再重新冷却，才符合“只为真实机会消耗 listings”的原则。

核心纪律：

> 429 只说明某条 Steam 路由正在限流，不代表 Cookie 失效，也不代表所有安全购买方式都不可用。熔断应该隔离故障路由，而不是把已经具备终态确认能力的整个交易状态机一起关闭。

## 2026-07-19：本地超时和离散快照都不能伪装成连续、权威的远端证据

### C5 发货确认事故风险

旧实现把 C5 发货期限写成 24 小时，并在订单详情暂时不可读、接口异常或者详情仍未返回终态时，仅凭本地时间超过期限就把补仓推进为 `c5_failed`，随后创建替换补仓。后端关闭后隔天重启时，旧任务会立即到期；如果启动瞬间 C5 详情接口还没恢复，就可能把真实存在且仍在处理的订单误判失败，形成重复补仓风险。

当前业务期限已经更正为 12 小时。固定纪律是：

```text
超过 12 小时
→ 查询 buyer_order_detail
→ 明确成功：completed
→ 明确失败：c5_failed，并创建唯一替换补仓
→ 仍在发货中：delivery_pending，继续复查
→ 详情不可读：delivery_pending，继续复查
```

12 小时只触发“到期复查”，不能单独授权失败或替换。后端重启后先经过短暂启动缓冲，再读取订单详情；C5 官方详情终态始终高于本地墙钟时间。

### 箱子槽位满载计时事故风险

旧实现没有保存每轮的满载观测，而是根据当前仍活跃的老挂单创建时间，反推槽位何时达到上限。因此系统即使中途关闭、Steam 快照长时间不可读，或者槽位曾经不满后又重新满载，只要当前老挂单年龄超过 3 小时，就可能立即随机撤销 12.5%。

连续满载必须由连续有效快照证明：

- 第一次完整确认满载时才写入 `fullSince`。
- 后续完整快照仍满、属于同一后端进程且观测间隔正常，才能延续计时。
- 任意一次确认不满，立即清空计时。
- 后端重启、Steam 快照读取失败、调度长时间中断或观测间隔过长，都使原连续证据失效。
- 状态未知的时间不得计算为满载时间；恢复后从第一次完整确认满载重新计算 3 小时。
- 达到 3 小时后仍要再次确认 Steam 远端挂单活跃，只有撤单成功才允许取消本地流水并释放资产。

核心纪律：

> 时间经过不等于远端业务失败；两个时点都为真，也不等于中间持续为真。会产生真实交易副作用的状态推进，必须依赖权威终态和连续可证明的观测链。

## 2026-07-22：不能把 Steam 旧 pricehistory 的 `+0` 机械当成 UTC

### 触发场景

调查 `StatTrak™ USP-S | Cyrex (Factory New)` 在北京时间 2026-07-22 03:25 左右是否真实成交时，旧接口 `/market/pricehistory/` 返回：

```text
Jul 21 2026 19: +0  ¥179.309  1
Jul 21 2026 20: +0  ¥147.483  1
```

如果把末尾 `+0` 直接当作 UTC，再固定加 8 小时，就会把 `¥179.309` 错分到北京时间 03:00，把 `¥147.483` 错分到 04:00。

Steam 新版市场页面实际把 price history 作为 SSR 预载数据写入初始 HTML，并使用 Unix 时间戳绘图。对应的权威点是：

```text
1784656800 → 2026-07-22 02:00 +08:00 → ¥179.309，1 件
1784660400 → 2026-07-22 03:00 +08:00 → ¥147.483，1 件
```

因此旧文本时间整体错了 1 小时。进一步把同一饰品的旧接口与新版页面逐点对照后，2016-11 至 2026-07 的 3762/3762 个点全部固定快 1 小时，冬季和夏季都一致。它不是“只在夏令时错”的季节性偏移。图表显示的北京时间 03:00、`¥147.48`、售出 1 件是正确的。

### 根因

旧接口日期字符串中的 `+0` 不是可以无条件信任的标准 UTC 偏移。项目里的 `_parse_price_history_time()` 原先把它直接替换为 `+0000`，导致所有 pricehistory 点错后 1 小时。

真正的问题不是“北京时间应该加几小时”，而是必须先确认数据源的时间语义。这个 Steam 旧标签经过十年数据交叉验证后可以执行专用的固定减 1 小时校正；但该校正不能推广给其他接口或地区时间。美国等地区仍存在夏令时和非夏令时切换，普通本地时间必须按具体日期和 IANA 时区转换。

### 固定规则

- Price history 时间优先使用 Steam 新版市场预载数据里的 Unix `time`，Unix 时间戳先按 UTC 解析，再通过 IANA 时区转换为 `Asia/Shanghai`。
- 禁止把旧接口文本末尾的 `+0` 机械替换为 `+0000`。当前解析器仅对已经用 3762 个 Unix 点验证过的 Steam 旧格式执行固定减 1 小时校正。
- 禁止手工固定加 7、8 或其他小时数来“修正”全年数据。
- 如果确实需要解析某个地区的本地时间，必须使用明确的 IANA 时区和该日期适用的夏令时规则，不能只保存固定 UTC offset。
- 其他旧文本或未知数据源如果无法与 Unix 时间戳交叉验证，必须标记小时级时间不确定，不能拿它证明某笔成交具体发生在哪个小时。
- 时间解析回归测试至少覆盖：夏令时开始前后、夏令时结束前后、非切换普通日期，以及最终北京时间归属。
- Price history 只能证明时间桶的中位价和成交量；即使某小时只有 1 件，也仍不能获得 listingId、买卖双方或精确成交秒数。

### 功能边界

`src/cs2_assistant/services/guadao_case_monitor.py` 属于独立的箱子挂刀比例监控与报告链路：采集 C5 价格、Steam orderbook 和 pricehistory，生成快照及 24h/7d/30d 流动性统计。它不是挂刀执行器状态机，不触发真实 Steam 上架、撤单、卖出推进或 C5 补仓，也不修改 `pool_operations`。

核心纪律：

> 涉及历史时间归属时，Unix 时间戳和时区数据库优先于看似带偏移的文本标签；夏令时规则必须按具体日期计算，不能靠固定加减小时猜测。

## 2026-07-26：`case_only` 命名有歧义，规范值改为 `crates_only`

### 事故现象

`Shanghai 2024 Vertigo Souvenir Package` 的旧 Steam 挂单撤销后，同一资产在比例重新符合时又被自动上架。排查时一度把 `case_only` 错误理解成“只允许名称以 `Case` 结尾的武器箱”，并手工只统计千瓦和变革，错误得出“上海纪念包没有计入箱子挂单槽、并且是它阻断了新挂单”的结论。

真实口径是：

- 正式配置值已经改为 `crates_only`，语义是 **CSGO-API crates only（仅广义箱子）**；旧 `case_only` 只作为兼容输入，读取后立即归一。
- CSGO-API `crates` 分类里的 Weapon Case、Sticker/Autograph Capsule、Souvenir/Collection Package、Container 等都属于该范围。
- 上海殒命大厦纪念包的当前 `sell_on_steam.listed` 流水已经计入活跃挂单数；槽未满时，这种 `listed` 状态本身不会阻断新挂单。

### 第二个误解：候选卡片上的等待原因不是本品类原因

扫描先计算整轮的 `blockedByOpenCycle`。只要存在一笔需要安全确认终态的阻断流水，本轮所有原本满足比例、库存和执行条件的候选都会显示 `waiting_existing_cycle`。

因此千瓦和变革同时显示“等待旧流水闭环”，不表示千瓦和变革各自因为同一笔旧单失败，也不表示候选卡片所展示的物品就是阻断源。2026-07-26 当时数据库存在多笔 `confirmationStatus=listing_missing_unverified` 的 `listing_pending`。继续核对后确认，旧实现把这些流水作为全局硬阻断，却又没有计入 150 个上架槽，造成“未满也不开新单”的容量逻辑矛盾。

正确模型不是“存在不确定状态就整轮关闭”的二元开关，而是分开维护两种容量：

- **新上架风险占用槽**：`listed + listing_missing_unverified`。合计未满 150 时继续扫描、新上架；达到 150 才暂停。
- **远端确认活跃槽**：只认 Steam 远端确认仍活跃的 `listed`。只有这个数量连续满载 3 小时，才允许随机撤销 12.5%。

`listing_missing_unverified` 占风险容量，是为了避免远端实际存在但本地没确认时超开；它不等于远端活跃证据，不能启动满载撤单。其他 `listing_pending`、品类状态与 operation 对不上、`rebuy_failed` 等仍然硬阻断。

### 固定规则

- 禁止根据名称后缀猜测 `crates_only`；优先使用 CSGO-API `csgoApi.categories` 中的 `crates` 分类，并让扫描、槽位、阻断、满载释放、报表和前端复用同一分类函数。
- `sell_on_steam.listed` 的广义箱子只占活跃挂单槽；低于 `caseMaxOpenGuadaoCount` 时不得仅凭它阻断下一轮。
- `listing_missing_unverified` 按数量计入风险槽但不再形成全局硬阻断；只有 `listed + listing_missing_unverified >= caseMaxOpenGuadaoCount` 才暂停新上架。
- 除 `listing_missing_unverified` 外的 `listing_pending`、缺少 operation 证据的孤立 `listing_pending`、需要人工确认的 `rebuy_failed` 等仍属于安全阻断，不能为了继续开单而绕过。
- 满载 3 小时随机释放只认 Steam 远端确认活跃的 `listed`，禁止把 `listing_missing_unverified` 当作活跃证据。
- `blockedByOpenCycle` / `waiting_existing_cycle` 是整轮全局结论。日志、API 和页面必须区分“候选为什么不可执行”与“究竟哪笔旧流水触发全局阻断”，不能把全局阻断原因写到候选卡片后就默认是该物品自身造成。
- 手工验算活跃槽位时必须覆盖所有广义箱子子类，不能只统计 Kilowatt Case、Revolution Case 等名称明显的 Weapon Case。

核心纪律：

> 容量风险和远端活跃证据必须分开建模；不确定 listing 可以占槽防止超开，但不能在槽未满时变成全局开关，也不能冒充远端活跃证据触发撤单。
## 2026-07-27：长效 Cookie 不应在每轮扫描中主动校验，未出队请求不能取消机会

### 事故现象

三笔 Profit Trade 已经锁定 A，并在 `search_listings` 返回 HTTP 400 后进入安全求购兜底。兜底在真正调用 `createbuyorder` 前需要重新读取 Steam orderbook，但该 P1 请求在本地队列等待时，被同时运行的 P0 Cookie 校验/登录阻塞并超时。三笔流水均没有发出 `buy_listing` 或 `createbuyorder`，旧外层却把队列超时当成普通购买失败，取消流水并释放 A。

两小时日志中出现 159 次 P0 Cookie 校验，说明根因不是 Cookie 本身寿命短，而是普通 Profit Trade 行情服务每次构建时都会主动校验全部账号。保存的长效 Cookie 被当成了每轮必须重新证明的临时凭据，制造了不必要的 P0 请求风暴。

### 固定规则

- 普通扫描和行情服务构建直接使用账号库中已保存的 Cookie，不主动校验全部账号。
- 只有具体账号的真实认证请求失败时，才由该请求链路刷新该账号 Cookie。
- `search_listings` HTTP 400 直接进入安全求购兜底，不启动全账号登录。
- `SteamRequestTimeout` 若发生在本地队列、且数据库能证明购买请求从未发出，首次立即从头重跑一次完整买前校验。
- 第二次仍超时时保留 `locked` 与 A 预留到下一周期后；下一轮必须重新读取 Steam/C5 行情、ROI、余额与风控，不能沿用旧价格直接购买。
- 只有状态仍为 `locked`、无购买请求/订单/资产证据、A 预留属于同一流水时才能保留并重试；存在任何远端请求可能已发出的证据时，禁止自动重买。
- 确定性的价格、ROI、余额或风控失败仍然取消，不得伪装成队列波动继续追单。

核心纪律：

> Cookie 是可复用凭据，不是每轮扫描的 P0 健康检查；本地队列超时证明“请求没获得执行机会”，不能被写成“Steam 已拒绝购买”。是否重试必须由不可逆购买证据边界决定。

## 2026-07-27：有界运营列表不能冒充“全部收益”，日期筛选必须由后端覆盖完整账本

### 严重事故

Profit Trade 页面把 `/api/profit-trade/dashboard` 返回的有界运营流水同时当成了已完结收益账本。该接口默认只读取最近 100 条原始流水，其中大量 `cancelled` 先占据名额，前端随后才把它们隐藏，并只对剩余数据做日期过滤。

事故发生时：

- 数据库真实存在 65 笔 `completed`。
- 全部 Steam 买入总额是 `CNY 8732.57`。
- 全部实际利润是 `CNY 493.66`。
- 页面只拿到 35 条可见流水，其中 34 笔 `completed`。
- 页面错误显示“34 / 34 笔”“全部收益 CNY 302.39”“Steam 买入总额 CNY 5578.09”。
- 被遗漏的 31 笔主要属于 2026-06-30 至 2026-07-11，遗漏利润合计 `CNY 191.27`。

这些旧记录没有被删除。真正的问题是：最近100条原始流水的窗口向前移动后，旧 `completed` 被后来产生的 `cancelled` 和其他流水挤出了 API；因此一个叫“全部累计”的数字会随着无关取消流水进入而下降。这属于严重报表真实性 Bug。

### 根因

错误设计混用了两种完全不同的数据产品：

1. 运营总览需要快速展示最近任务、进行中和异常流水，可以设置合理上限。
2. 会计统计需要回答全部累计、选定日期的笔数和金额，必须覆盖完整筛选范围。

前端收到截断后的运营列表，再在浏览器里筛选日期，不可能找回已经被后端截掉的历史记录。把 `limit` 从 100 临时改成 500 也不是修复；数据继续增长后同一事故一定会重现。

### 固定规则

- 凡是页面写“全部”“累计”“总收益”“总金额”或允许日期筛选，统计数据必须来自后端完整范围查询，禁止复用最近流水数组。
- 日期选择必须下沉到后端：前端把本地日期转换成 UTC 起止时间，后端按每笔真实 `steamBoughtAt` 查询。
- Profit Trade 收益日期只认 Steam 实际购买 B 的时间，不认创建流水、最后更新或 C5 卖出完成时间。
- 状态条件和日期条件必须先于分页；分页只控制明细，不得改变总笔数、总利润和总买入额。
- `cancelled`、`failed` 和活动流水不能占用 `completed` 查询容量。
- “全部”不得带未声明的行数上限。若产品确实只能展示最近 N 条，必须明确写“最近 N 条”，绝不能写“全部”。
- 汇总与明细必须基于同一份筛选谓词和同一时间口径。
- 没有可靠购买时间的旧记录在“全部”中保留；日期筛选时不伪造时间。
- 回归测试必须让 completed 落在普通列表上限之外，并用超过上限的 cancelled 制造挤压；只有旧 completed 仍被完整统计，测试才算有效。
- 验收必须直接对账：数据库 completed 数量、`SUM(realized_profit)`、`SUM(steam_buy_price)`与 API 返回值逐项一致。

### 本次修复后的验收基线

```text
全部 completed：65 笔
实际利润合计：CNY 493.66
Steam 买入合计：CNY 8732.57

北京时间 2026-07-11：
completed：2 笔
实际利润：CNY 41.09
Steam 买入：CNY 542.99
```

回归测试额外构造 2 笔 completed 和 101 笔 cancelled，证明取消流水无法再把已完结收益挤出；前端日期切换会重新请求后端，不再对最近运营列表做“假筛选”。

核心纪律：

> 有界运营列表不是会计账本。任何“先取最近 N 条，再过滤状态或日期”的统计都会漂移；全部和累计只能从完整筛选集合计算。

## 2026-07-28：MyListings 单页不能冒充全部远端挂单，强弱证据必须分阶段提交

### 严重事故

挂刀账号 `xiaodigu11` 的 Steam `mylistings` 明确返回 `total_count=105`、`num_active_listings=105`，但旧 `list_active_listings()` 默认只请求 `start=0&count=100`，随后直接把第一页100条当成账号全部远端活跃挂单。

实际第二页 `start=100` 还有5笔：

- 3笔 Fever Case；
- 2笔 Kilowatt Case。

这5笔已经在 Steam 远端活跃，却因不在第一页而长期停在 `confirm_sent_waiting_active_listing`。更严重的是，账号同步把这些“第一页没看到”的活跃流水错误送入 `myhistory`。活跃挂单当然没有成交回执，于是历史目标永远找不齐，程序连续深翻成交历史；实测每页100条、约第21页触发429，并进一步打开 Steam 全局熔断。

旧同步顺序还是：

```text
读取单页 MyListings
→ 查询全部未解决流水的 MyHistory
→ MyHistory异常时整轮直接失败
→ 前面已经读到的真实活跃挂单也不落库
```

因此一个更弱、更慢、容易限流的后序证据源，反过来吞掉了前面已经取得的强正向证据。

### 根因

1. 把 API 的默认分页大小误当成完整数据边界。
2. 只返回裸 `list`，没有同时返回 `total_count`、实际读取数和 `complete`，调用方无法区分“确实不存在”和“只是没翻到”。
3. `MyListings` 与 `MyHistory` 共用一个失败边界，没有按证据强弱分阶段提交。
4. 活跃挂单被错误加入成交历史目标，制造无法满足的深翻条件和429请求风暴。
5. 多笔待确认流水逐笔重复读取 `mylistings/mobileconf`，请求次数随流水数量线性增长。

### 固定规则

- API默认分页大小不是完整数据。任何“远端不存在”判断，必须先证明分页已经覆盖官方总数。
- `list_active_listings()` 无参数必须读取账号全部 MyListings：每页100条，按 `start=0、100、200……` 翻页，按 `listingId` 去重，直到实际读取数覆盖官方总数。
- 显式 `count=N` 只能表示轻量查询的总结果上限，不能被状态机当成完整远端快照。
- 完整快照必须同时保存活跃挂单、`listings_to_confirm`、官方总数、实际读取数、页数、观察时间和 `complete`。
- 总数中途变化、空中间页、页码不连续或后页失败时，已精确匹配的正向证据仍然有效；但 `complete=false`，未匹配结果绝不能用于卖出、撤销、释放库存或补仓。
- 证据链必须严格分层：

  ```text
  完整 MyListings
  → 活跃/待确认逐笔匹配并先落库
  → 只有仍未解决的流水进入 MyHistory
  ```

- 多层证据链必须按强弱分阶段提交。后续 MyHistory 的429、异常或覆盖不足，不能回滚或吞掉 MyListings 已确认的 `listed`。
- 活跃挂单和 `listings_to_confirm` 绝不能进入成交历史目标；否则会制造永远找不到的目标、无意义深翻和429风暴。
- MyHistory 每页固定500条。实测 `count=500` 正常，`count=1000` 会异常返回空结果，禁止为了少翻页使用1000。
- MyHistory 找齐全部目标，或历史时间已经覆盖到最早目标上架时间附近后，应立即停止；时间字段不可读或覆盖不足时，未找到只能延期，不能判定不存在。
- MyHistory 后页失败时，前页已经精确找到的成交回执仍可推进对应流水；其他流水保持待确认，并按 circuit `retryAt` 或正常证据检查间隔重排。
- 同账号多笔待确认必须账号级批量处理：复用同一轮 `listings_to_confirm`，一次读取移动确认列表，一次提交明确匹配的 confirmation；禁止逐笔重复读取。

核心纪律：

> “这一页没看到”不等于“远端不存在”。强证据先独立落库，弱证据只处理剩余问题；后序接口失败永远不能抹掉前序已经证明的事实。

## 2026-08-07：饰品搜索前50条不能冒充全部候选

### 事故现象

保护品类搜索框输入“折叠刀”时，只显示 StatTrak 版本，看不到数据库中明确存在的普通版“折叠刀（★） | 伽玛多普勒”。

实查本地目录共有202条折叠刀：旧接口按 `name_cn ASC` 排序后直接 `LIMIT 50`，第1～101条全部是 StatTrak，普通版从第102条开始，普通版伽玛多普勒位于第113、114条。前端拿到的只有前50条，因此无论怎样滚动都不可能找到目标。

### 根因

1. 把数据库字典序当成搜索相关性排序。
2. 把首批返回上限当成完整候选集合，没有返回总数和继续游标。
3. 普通版和 StatTrak 没有按同一饰品归组，某一版本可以垄断首屏。
4. 整句 `%keyword%` 只支持连续子串，不支持用户自然输入的多关键词。
5. 目录名称没有字面“普通版”，且“伽马/伽玛”等常见写法不同，旧搜索没有语义归一化。

### 固定规则

- 所有标准饰品目录搜索统一使用分词、别名归一、版本语义和相关性排序。
- 普通版与 StatTrak 同款相邻展示，首屏不得被单一版本批量挤占。
- API 必须返回 `offset、limit、total、hasMore、nextOffset`；前端必须提供“加载更多结果”。
- 翻页合并按 `marketHashName` 去重；查询词变化后必须清空旧游标，迟到响应不能覆盖新查询。
- 搜索接口的 `limit` 只表示一页大小，绝不表示全部结果。
- 修复共享目录搜索时必须核对所有调用入口，不能只修用户截图中的一个弹窗。
- 测试必须构造结果数超过一页、目标位于旧截断边界之后的场景，并验证首屏相关性、普通/StatTrak语义、中文别名、第二页无重复。

核心纪律：

> 排序负责让正确结果尽早出现，分页负责保证全部结果最终可达；两者缺一不可。

## 2026-08-07：`products/search` 前50件不能证明 C5 真实最低挂单不存在

### 事故现象与一手证据

`Austin 2025 Legends Autograph Capsule` 有4笔冻结最高补仓价均为 CNY 1.63 的待补仓流水。相同时间窗口内取得的结果是：

```text
price_batch：最低价 CNY 1.62，在售数量约 2900 件
C5 App：可见普通在售 CNY 1.62
products/search：只传 itemId + pageSize=50，返回50件且全部为 CNY 1.64
products/list：不传 delivery / acceptBargain，第一页同样从 CNY 1.64 开始
```

`itemId` 分别以字符串和整数传入都不能取得 CNY 1.62；显式传入或删除 `delivery`、`acceptBargain` 也不能消除差异。稍后低价挂单消失后，`price_batch` 与两个逐单接口重新同时显示 CNY 1.64。

因此这次问题不是本地 ROI、冻结补仓价或逐单匹配算法把 CNY 1.62 筛掉，而是 C5 品类聚合最低价与内测逐单接口在短时间内没有提供一致的数据视图。`products/search` 返回的前50件不能冒充 `price_batch` 所统计市场的完整最低50件，也不能用“逐单结果为空”证明价格上限内确实没有可购买商品。

### 固定实现规则

- C5 品类最低价和是否进入补仓价格区间继续以 `merchant/product/price/batch` 为准；`market/v2/products/search` 只负责提供可见逐单商品的 `productId`。
- 逐单请求只传物品标识和页大小，不得传 `delivery` 或 `acceptBargain`。返回前50件仍只是一份有界逐单快照，不具备否定 `price_batch` 最低价的权力。
- 只有 `price_batch` 最低价严格低于逐单前50件最低价时，才判定存在“聚合可见、逐单漏出”的低价带；两者相等或逐单更便宜时直接走正常逐单批量购买。
- 如果逐单结果为空，则把逐单最低价视为未知，不把它当作市场无货；只要 `price_batch` 仍不高于某笔流水的冻结补仓价，就允许进入有界 `quick_buy` 兜底。
- `quick_buy` 按冻结补仓价最严格且仍能接受当前最低价的流水优先。每笔必须使用对应 Steam 账号的真实 `tradeUrl`，并在请求前持久化唯一 `outTradeNo`。
- 当逐单最低价已知时，兜底 `quick_buy.maxPrice` 必须取“本单冻结补仓价”和“逐单最低价减 CNY 0.01”中的较小值。这样 quick buy 只处理逐单接口漏掉的低价带，不会抢先消费随后要交给批量购买的可见商品。
- 每次明确成功后才处理下一笔；第一次明确返回无符合价格商品时立即结束 quick buy 循环。响应超时、缺少订单证据或结果不确定时必须停止该品类全部后续购买并进入既有订单对账，禁止继续 quick buy 或批量购买。
- quick buy 循环次数最多等于本轮仍符合条件的待补仓流水数，禁止因 `price_batch` 缓存未及时变化而无界重试。
- 第一次调用 `products/search` 时必须在本轮内保存完整的前50件快照，包括每件的 `productId`、价格以及购买匹配所需字段；`concreteFloor` 只是从这份快照派生的比较值，不能只保存最低价后丢弃其余49件。
- quick buy 结束后，剩余流水必须直接复用本轮已经保存的前50件快照，继续正常逐单匹配并按 `tradeUrl` 分组批量购买；禁止为了进入正常链路再次调用 `products/search`。兜底 quick buy 的上限严格低于该快照最低价，因此不会主动消费快照中的可见 `productId`；若快照中的商品随后被其他买家买走，则以 C5 批量购买返回的 `failedList` 安全失败。
- 最终只按 C5 返回的 `successList` 推进成功流水。
- 日志必须同时记录 `priceBatchFloor`、`concreteFloor`、差异金额、quick buy 尝试/成功/明确无货/终态未知数量，以及随后正常批量购买的成功数量。

### 后续兼容性

该兜底由实时价格差异触发，不永久写死某个饰品或 CNY 1.62。以后 C5 修复 `products/search` 后，两边最低价一致，程序会自然跳过 quick buy，全部回到效率更高的逐单批量购买路径。

核心纪律：

> 聚合最低价证明市场可能存在这个价格，逐单前50件只证明接口本轮返回了这些商品。两个数据源短暂不一致时，用冻结价格保护下的有界 quick buy 补缺口；任何不确定购买结果都必须先对账，不能继续叠加真实请求。

## 2026-08-08：Steam 钱包下降后旧求购仍可能留在榜单

### 真实现象

小嘀咕账号有两张 Steam 求购：较低价的“剑片琥珀（崭新）”先成交，钱包随后约剩 CNY 286；最高愿付价约 CNY 465 的“骨铜密码（崭新）”求购仍显示在 Steam 求购榜。

### 这次实验真正证明的事

1. Steam 求购的远端存活状态与当前钱包余额不是同一个状态。
2. 钱包余额后来下降，不保证旧求购立即自动撤销或从榜单消失。
3. 求购仍在榜上，不等于当前一定有足够资金成交，也不等于资金已经完整预留。
4. 真正遇到卖家供给时，Steam 才会再次面对付款能力；余额不足可能导致撮合失败或后续清理。
5. “超过钱包余额就会立刻自动撤单”不能再作为程序假设。

### 对程序的固定教训

- 创建新求购前，必须按账号当前可用钱包核对单笔最高实际付款额；不能按 ROI 折扣后成本判断余额。
- 旧求购成交一笔后，必须重新读取钱包并重新评估剩余求购的资金健康度；不能只看它们是否还显示在榜单。
- 余额不足时，程序不得新增求购，也不得把旧单继续标记为“健康等待”；但在没有完整远端终态证据时，也不能擅自把它当作已撤单。
- 求购终态必须联合钱包、远端活跃求购、库存和 Steam 官方 market history 判断；`createbuyorder` 成功只说明创建成功，求购榜展示只说明记录仍可见。
- 本次实验没有覆盖 Steam 在下一笔卖家供给到来时的具体失败响应，因此不要把“可能撮合失败/后续清理”写成确定的自动行为。

核心纪律：

> 求购榜是订单记录，不是余额证明；钱包是当前支付能力，不是旧求购的自动清理器。余额变化后，旧求购必须重新核对，任何未确认终态都不能直接当作成功或撤销。
