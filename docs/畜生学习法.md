ts学习
balanceDiscount?: number;
balanceDiscount 可以不存在

var 可以重复声明，let 不可以
let 是块级作用域，var 是函数作用域

const 限制的是“变量绑定不能变”，不是“值本身完全不能变”。
js
反引号
`--item "${itemName.value.trim()}"`  反引号`-模板字符串,要用$



在 JavaScript / TypeScript 里，只有下面这些值是假值，也就是 if (...) 里会被当成 false：

false
0
-0
0n
""
null
undefined
NaN

空对象也是真,因为有引用值


function refreshPreview(): void {
  rows.value = [...rows.value].sort((a, b) => b.c5Cash - a.c5Cash);
}
[...数组]的语法等价于复制一个新数组
sort函数的参数需要接收的是一个比较函数，现在有点能理解，容易接受了！







github
git push -u origin feature/trae
一些核心命令，psuh，远程合并，拉取。pulldownmain，哈哈



html基础知识
tr -> table row 表格的一行
td -> table data  表格数据单元格  
span：行内标签，不会独占一行  加东西的
div：块级标签，通常会独占一行

<label class="checkbox-row">
  <input v-model="detail" type="checkbox" />
  Detail
</label>
label嵌套式写法，点detail，复选框也会选中
label还有for对应的id写法


vue学习
@ = v:on 事件监听
：= v-bind 数据 / 属性绑定    把 JS/TS 里的表达式结果，绑定到模板里的某个属性上
v-model   表单双向绑定，输入框和变量互相同步

computed(() => 123)              // ComputedRef<number>
computed(() => "abc")            // ComputedRef<string>
computed(() => true)             // ComputedRef<boolean>

<tr v-for="row in filteredRows" :key="row.name">
v-for 数组循环


template 中 Vue 会自动解包，因此不写 .value

PYTHON基础核心思想

定义函数
def ssr(a,b):
  return x,y
  等价于
  return(x,y)这个元组！！！
返回多参数
def ssr(a,b):
  pass
什么也没有
jkl = ssr
函数引用赋值



set集合{xxxxxx}
tuple元组(xxxxxxxxx)
list数组[xxxxxxxx]




# 一级标题
## 二级标题
### 三级标题

- 普通列表
  - 二级列表

- [ ] 未完成任务
- [x] 已完成任务

**加粗内容**

> 重要结论或引用

---